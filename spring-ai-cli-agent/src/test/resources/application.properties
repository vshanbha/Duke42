# Test profile: the Spring Shell interactive runner must not start (and block on
# stdin) while a Spring context is booted by integration/eval tests.
spring.shell.interactive.enabled=false
spring.main.web-application-type=none
