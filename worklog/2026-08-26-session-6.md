# Session 6 — 2026-08-26: softwareaifactory.sh installed (java pack)

## What was done
- Fetched installer, inspected before running (their recommended practice), bare
  fetch into `~/.software-factory-template` @ pinned `v0.1.5`.
- Ran `factory-init.sh "$PWD" --pack java` non-interactively (documented stdin fallback).
  Reconciled brownfield collisions: restored project README/AGENTS.md/ci.yml from HEAD,
  merged `.gitignore`, deleted Gradle-only artifacts (`quality.gradle`, `java-pack.yml`)
  per Decision #2 in docs/DECISION_LOG.md.
- Armed Maven-native check_command after the default Gradle one proved wrong for
  this repo; first version leaked failsafe-only E2EIT into surefire via `-Dtest`
  include replacement — fixed with explicit exclusion list (caught by running it,
  exactly what the gate is for).
- CI: added `factory-gates` job running the break/fix selftest on every push/PR.
- Push gate armed: `git config core.hooksPath .githooks`.
- AGENTS.md: factory section added; role/test-edit contradiction resolved
  (roles hook-enforced now, ordinary sessions still write tests+impl together).

## Verification (RAN/OBSERVED)
- init attestation: `selftest: 141 passed, 0 failed`; "gates proven and armed for: java"
- `make check` → selftest + hook-existence + wiki-lint + workflow-lint all pass
  (hook-existence initially FAILED pre-commit with 22 untracked scripts — correct behavior)
- `./factory doctor` → "healthy — every armed gate is live and proven" (10 ARMED, 2 inert-by-design)
- Gate command: `mvn -q test '-Dtest=!ChatClientIntegrationTest,!E2EIT' ...` BUILD SUCCESS
  + junit5-only-check PASS ("all Java behavioral tests use JUnit 5")
- `./factory report` shows 14 deterministic gates, 1 recorded block (the pre-commit
  hook-existence catch)

## Adoption friction observed (filed upstream)
- First check_command draft used `sh scripts/hooks/junit5-only-check.sh` → POSIX sh
  parse error at line 34 (`< <(find …)` process substitution is bash-only). Direct
  execution via shebang works. Filed as anoop2811/software-factory-template#67.
- Deleting the whole `packs/` dir made selftest die mid-suite with a raw
  `cp: packs/review-lane/review-pr.yml: No such file or directory` (no test name,
  no guidance). Restored that one file; Gradle artifacts stayed deleted.
  Filed as #68. Maven adaptation overall filed as #66.

## Notes / follow-ups
- Java pack quality plugins (Spotless/Error Prone/SpotBugs/PIT) not wired into
  Maven poms yet — Decision #2 defers java-pack.yml until they land.
- Model tiers left on `inherit`; wire a distinct reviewer model in factory.yaml
  when a second provider is available.
